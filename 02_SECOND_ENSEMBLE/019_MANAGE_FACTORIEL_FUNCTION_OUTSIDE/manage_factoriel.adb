with saisi_limite;

with for_loop_calculate;

procedure manage_factoriel is
   
   -- C : Integer;
   -- limit : Integer;
	
   begin
      -- limit := saisi_limite(1);
      for_loop_calculate(saisi_limite(1));
end manage_factoriel;